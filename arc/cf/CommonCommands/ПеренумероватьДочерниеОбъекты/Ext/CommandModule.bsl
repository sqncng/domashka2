
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ПараметрКоманды = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВозниклиОшибки = Ложь;
	
	Универсальные.Перенумеровать(ПараметрКоманды, ВозниклиОшибки, Истина);
	ОповеститьОбИзменении(ТипЗнч(ПараметрКоманды));
	
	Если Не ВозниклиОшибки Тогда
		ТекстОповещения = НСтр("ru='Перенумерация выполнена'");
		ПоказатьОповещениеПользователя(ТекстОповещения);
	КонецЕсли;
	
КонецПроцедуры
