#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// См. УправлениеДоступомПереопределяемый.ПриЗаполненииСписковСОграничениемДоступа.
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт

	Ограничение.Текст =
	"РазрешитьЧтениеИзменение
	|ГДЕ
	|	 ВЫБОР КОГДА ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ВидыДоступа) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ОбработчикиОбновленияИнформационнойБазы) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ОбъектыМетаданных) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.Ошибки) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.Подсистемы) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ПрофилиПользователей) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.Процессы) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ТехническиеПроекты) ИЛИ ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.Идеи) ТОГДА ЗначениеРазрешено(ВерсионируемыйОбъект.Владелец)
	|	КОГДА ТипЗначения(ВерсионируемыйОбъект) = Тип(Справочник.ШагиПроцесса) ТОГДА ЗначениеРазрешено(ВЫРАЗИТЬ(ВерсионируемыйОбъект КАК Справочник.ШагиПроцесса).Владелец.Владелец)
	|	ИНАЧЕ ВерсионируемыйОбъект <> Неопределено КОНЕЦ ";

КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#КонецОбласти

#КонецЕсли